import { RouterConfig } from '@angular/router';

import { PresetsComponent } from './index';

export const PresetsRoutes: RouterConfig = [
  {
    path: 'presets',
    component: PresetsComponent
  }
];
